export default class Career {
  constructor(
    public id: number,
    public code: string,
    public enf: string,
    public plan: number,
    public name: string,
    public isChecked: boolean,
  ) {}
}
